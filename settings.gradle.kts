pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "moonlight-personal"

include(":app")
include(":shared")
include(":desktopApp")

